对于一个新的变量，也许应该关注其引入和消去。如min
引入：
- #check (le_min : c ≤ a ! c ≤ b ! c ≤ min a b)
消去：
- #check (min_le_left a b : min a b ≤ a)
- #check (min_le_right a b : min a b ≤ b)

argument是参数的意思。multiple argument为多参数。Lean4中，多参数问题都会被currying处理。

- 了解了repeat策略。重复接下来的apply或者整个block操作。
- 使用let、have等定义变量或命题时，确保在分支之前。否则可能会被清理掉。