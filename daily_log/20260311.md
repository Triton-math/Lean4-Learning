小结：完成了Basic section3. 但感觉这些学习都还是浅尝辄止。要多练习才行。

- norm_num是tactic，不需要用apply。apply本身是tactic，后面跟定理。
- rw只能用等式改写，不能用不等式条件改写。
- 注意apply 充要命题的时候，应该用定理本身还是其mpr（反向蕴含）
- 用 constructor 可以直接把命题\and提取成为两个子命题
- 用到的几个策略
    - linarith：
    - norm_num：处理具体的数值计算/比较
    - theorem在section中声明定理，并且使用它
    - 把后面需要反复用到的引理做出声明
- 一些常用处理
    - 绝对值
        - abs_le或者abs_le'
    - pow_two_nonneg 平方非负
    - le_div_iff₀ 去分母