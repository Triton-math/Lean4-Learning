遇到了一个很神奇的例子：

example : Surjective f ↔ RightInverse (inverse f) f := by
  constructor
  · intro fSur x
    rcases fSur x with ⟨f_invx, h⟩
    rw[inverse, dif_pos ⟨f_invx, h⟩]
    exact Classical.choose_spec ⟨f_invx, h⟩

编译无法通过，但

example : Surjective f ↔ RightInverse (inverse f) f := by
  constructor
  · intro fSur x
    rcases fSur x with h
    rw[inverse, dif_pos h]
    exact Classical.choose_spec h

却可以。Gemini声称这是因为将命题 fSur x 先拆解再重组会无法匹配，并建议使用obtain获取而不是rcases拆解。- 但还是没有给出一个明确的解释，也没有给出一个明确的解决方法。

完成了 Sets & Funs section 2

